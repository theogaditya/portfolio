import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'portfolio',
  webDir: 'public',
   server: {
    url: 'https://www.adityahota.online/', 
  }
};

export default config;
